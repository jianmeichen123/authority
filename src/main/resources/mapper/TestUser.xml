<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.galaxy.authority.dao.user.ITestUserMapper">
	
	<select id="getFileByid" parameterType="integer" resultType="TestUserBean" resultMap="testUserMap">
		select * from test_user where id=#{id};
	</select>
	
	<insert id="saveUser" parameterType="TestUserBean" keyColumn="id">
		insert into test_user(name,email) values(#{name},#{email});
	</insert>
	
	
	<resultMap type="TestUserBean" id="testUserMap">
		<id column="id" property="id"/>
		<result column="name" property="name"/>
		<result column="email" property="email"/>
	</resultMap>
	
	
	
	

</mapper>